apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "autoscale-service.fullname" . }}-test-connection"
  labels:
    {{- include "autoscale-service.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "autoscale-service.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
