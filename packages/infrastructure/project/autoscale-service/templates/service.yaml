apiVersion: v1
kind: Service
metadata:
  name: {{ include "autoscale-service.fullname" . }}
  labels:
    {{- include "autoscale-service.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "autoscale-service.selectorLabels" . | nindent 4 }}
